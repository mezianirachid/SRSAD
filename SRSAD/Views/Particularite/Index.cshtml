@model IEnumerable<SRSAD.Models.ParticularitesUsagerRef>

@{
    ViewBag.Title = "Particularités des patients";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-md-12">
            <h2>
                Particularités des patients
                <small class="pull-right">
                    @Html.ActionLink("Ajouter une particularité", "Create", null, new { @class = "btn btn-success" })
                </small>
            </h2>
            <hr />
        </div>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible">
            <button type="button" class="close" data-dismiss="alert">&times;</button>
            @TempData["Success"]
        </div>
    }
    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger alert-dismissible">
            <button type="button" class="close" data-dismiss="alert">&times;</button>
            @TempData["Error"]
        </div>
    }

    <div class="row">
        <div class="col-md-12">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <h3 class="panel-title">Recherche</h3>
                </div>
                <div class="panel-body">
                    @using (Html.BeginForm("Index", "Particularite", FormMethod.Get, new { @class = "form-inline" }))
                    {
                        <div class="form-group">
                            @Html.TextBox("searchLibelle", Request.QueryString["searchLibelle"], new { @class = "form-control", placeholder = "Libellé" })
                        </div>
                        <div class="checkbox">
                            <label>
                                @Html.CheckBox("actifUniquement", Request.QueryString["actifUniquement"] != "False")
                                Actifs uniquement
                            </label>
                        </div>
                        <button type="submit" class="btn btn-primary">Rechercher</button>
                    }
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-md-12">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <h3 class="panel-title">Liste des particularités (@Model.Count())</h3>
                </div>
                <div class="panel-body">
                    <table class="table table-striped table-hover">
                        <thead>
                            <tr>
                                <th>Code</th>
                                <th>Libellé</th>
                                <th>Niveau d'alerte</th>
                                <th>Statut</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td><strong>@item.Code</strong></td>
                                    <td>@item.Libelle</td>
                                    <td>
                                        @if (item.NiveauAlerte == 0)
                                        {
                                            <span class="label label-default">Info</span>
                                        }
                                        else if (item.NiveauAlerte == 1)
                                        {
                                            <span class="label label-info">Alerte</span>
                                        }
                                        else if (item.NiveauAlerte == 2)
                                        {
                                            <span class="label label-warning">Critique</span>
                                        }
                                        else
                                        {
                                            <span class="label label-danger">Urgent</span>
                                        }
                                    </td>
                                    <td>
                                        @if (item.EstActif)
                                        {
                                            <span class="label label-success">Actif</span>
                                        }
                                        else
                                        {
                                            <span class="label label-default">Inactif</span>
                                        }
                                    </td>
                                    <td>
                                        @Html.ActionLink("Détails", "Details", new { id = item.ParticulariteID }, new { @class = "btn btn-xs btn-info" })
                                        @Html.ActionLink("Modifier", "Edit", new { id = item.ParticulariteID }, new { @class = "btn btn-xs btn-warning" })
                                        @using (Html.BeginForm("Delete", "Particularite", new { id = item.ParticulariteID }, FormMethod.Post, new { style = "display:inline", onsubmit = "return confirm('Supprimer cette particularité ?');" }))
                                        {
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-xs btn-danger">Supprimer</button>
                                        }
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>