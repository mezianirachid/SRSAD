@model IEnumerable<SRSAD.Models.StatutsSuivi>

@{
    ViewBag.Title = "Statuts de suivi";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-md-12">
            <h2>
                Gestion des statuts de suivi
                <small class="pull-right">
                    @Html.ActionLink("Ajouter un statut", "Create", null, new { @class = "btn btn-success" })
                    <button type="button" class="btn btn-info" onclick="exportToExcel()">
                        <span class="glyphicon glyphicon-export"></span> Exporter
                    </button>
                </small>
            </h2>
            <hr />
        </div>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible">
            <button type="button" class="close" data-dismiss="alert">&times;</button>
            @TempData["Success"]
        </div>
    }
    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger alert-dismissible">
            <button type="button" class="close" data-dismiss="alert">&times;</button>
            @TempData["Error"]
        </div>
    }

    <div class="row">
        <div class="col-md-12">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <h3 class="panel-title">Recherche</h3>
                </div>
                <div class="panel-body">
                    @using (Html.BeginForm("Index", "StatutSuivi", FormMethod.Get, new { @class = "form-inline" }))
                    {
                        <div class="form-group">
                            @Html.TextBox("searchCode", Request.QueryString["searchCode"], new { @class = "form-control", placeholder = "Code" })
                        </div>
                        <div class="form-group">
                            @Html.TextBox("searchLibelle", Request.QueryString["searchLibelle"], new { @class = "form-control", placeholder = "Libellé" })
                        </div>
                        <div class="checkbox">
                            <label>
                                @Html.CheckBox("actifUniquement", Request.QueryString["actifUniquement"] != "False")
                                Actifs uniquement
                            </label>
                        </div>
                        <button type="submit" class="btn btn-primary">
                            <span class="glyphicon glyphicon-search"></span> Rechercher
                        </button>
                        @Html.ActionLink("Réinitialiser", "Index", null, new { @class = "btn btn-default" })
                    }
                </div>
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-md-12">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <h3 class="panel-title">
                        Liste des statuts de suivi
                        <span class="badge">@Model.Count()</span>
                    </h3>
                </div>
                <div class="panel-body">
                    <table class="table table-striped table-hover" id="statutsTable">
                        <thead>
                            <tr>
                                <th>Code</th>
                                <th>Libellé</th>
                                <th>Statut</th>
                                <th>Assignations</th>
                                <th class="text-center">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr class="@(item.EstActif ? "" : "text-muted")">
                                    <td><strong>@item.Code</strong></td>
                                    <td>@item.Libelle</td>
                                    <td>
                                        @if (item.EstActif)
                                        {
                                            <span class="label label-success">Actif</span>
                                        }
                                        else
                                        {
                                            <span class="label label-default">Inactif</span>
                                        }
                                    </td>
                                    <td>
                                        @{
                                            var count = ViewBag.Statistiques != null ?
                                                ((Dictionary<int, int>)ViewBag.Statistiques).ContainsKey(item.StatutSuiviID) ?
                                                ((Dictionary<int, int>)ViewBag.Statistiques)[item.StatutSuiviID] : 0 : 0;
                                        }
                                        <span class="badge">@count</span>
                                    </td>
                                    <td class="text-center" style="white-space: nowrap;">
                                        @Html.ActionLink("Détails", "Details", new { id = item.StatutSuiviID }, new { @class = "btn btn-xs btn-info" })
                                        @Html.ActionLink("Modifier", "Edit", new { id = item.StatutSuiviID }, new { @class = "btn btn-xs btn-warning" })
                                        @if (item.EstActif)
                                        {
                                            using (Html.BeginForm("Desactiver", "StatutSuivi", new { id = item.StatutSuiviID }, FormMethod.Post, new { style = "display:inline", onsubmit = "return confirm('Désactiver ce statut de suivi ?');" }))
                                            {
                                                @Html.AntiForgeryToken()
                                                <button type="submit" class="btn btn-xs btn-default">
                                                    <span class="glyphicon glyphicon-ban-circle"></span> Désactiver
                                                </button>
                                            }
                                        }
                                        else
                                        {
                                            using (Html.BeginForm("Activer", "StatutSuivi", new { id = item.StatutSuiviID }, FormMethod.Post, new { style = "display:inline", onsubmit = "return confirm('Activer ce statut de suivi ?');" }))
                                            {
                                                @Html.AntiForgeryToken()
                                                <button type="submit" class="btn btn-xs btn-success">
                                                    <span class="glyphicon glyphicon-ok-circle"></span> Activer
                                                </button>
                                            }
                                        }
                                        @using (Html.BeginForm("Delete", "StatutSuivi", new { id = item.StatutSuiviID }, FormMethod.Post, new { style = "display:inline", onsubmit = "return confirm('Êtes-vous sûr de vouloir supprimer définitivement ce statut de suivi ?');" }))
                                        {
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-xs btn-danger">
                                                <span class="glyphicon glyphicon-trash"></span>
                                            </button>
                                        }
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

@section scripts {
    <script>
        $(document).ready(function() {
            // Initialiser DataTable pour de meilleures fonctionnalités de tri et recherche
            $('#statutsTable').DataTable({
                "paging": true,
                "ordering": true,
                "info": true,
                "language": {
                    "url": "//cdn.datatables.net/plug-ins/1.10.20/i18n/French.json"
                },
                "columnDefs": [
                    { "orderable": false, "targets": 4 }
                ]
            });
        });

        function exportToExcel() {
            var table = document.getElementById('statutsTable');
            var html = table.outerHTML;
            var url = 'data:application/vnd.ms-excel,' + escape(html);
            var link = document.createElement("a");
            link.download = "statuts_suivi_" + new Date().toISOString().slice(0,10) + ".xls";
            link.href = url;
            link.click();
        }

        // Rafraîchir les statistiques toutes les minutes
        setInterval(function() {
            $.get('@Url.Action("GetStatsUtilisation", "StatutSuivi")', function(data) {
                // Mettre à jour les badges
                data.forEach(function(item) {
                    $('tr[data-id="' + item.StatutSuiviID + '"] .badge').text(item.NombreAssignations);
                });
            });
        }, 60000);
    </script>
}